<h1><%= @product.description %></h1>

<p><%= @product.barcode %></p>
<p><%= @product.price %></p>



<ul>
  <li><%= link_to "Edit", edit_product_path(@product) %></li>
    <li><%= link_to "Destroy", product_path(@product), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>

    <li><%= link_to "Destroy", product_path(@product)%></li>
</ul>
 <%= button_to "Delete", product_path(@product), method: :delete, class: "btn btn-outline-primary mt-2"%>

<li><%= link_to "Products", products_path%></li>